import styles from './empty-elements.module.css';

export const EmptyElements = () => {
  return (
    <div className={styles.emptyElements}>
      <span className="text text_type_main-default">Выберите начинку</span>
    </div>
  )
}
